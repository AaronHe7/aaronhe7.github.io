#include <bits/stdc++.h>
using namespace std;
#define setio(file) freopen(&(string(file) + ".in")[0], "r", stdin); freopen(&(string(file) + ".out")[0], "w", stdout);

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
}
